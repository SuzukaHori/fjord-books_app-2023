<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('helpers.submit.update', model: t('book')), edit_book_path(@book) %> |
  <%= link_to t('button.back', model: t('book')), books_path %>
  <%= button_to t('button.destroy', model: t('book')), @book, method: :delete %>
</nav>
