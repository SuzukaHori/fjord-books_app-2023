   <%= form_with model: [commentable, Comment.new] do |f| %>
   <%= f.label :content, style: "display: block" %>
   <%= f.text_field :content %>
<div class="actions">
   <%= f.submit "コメントする" %>
</div>
<% end %>
