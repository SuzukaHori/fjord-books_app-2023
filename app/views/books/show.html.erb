<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('helpers.submit.update', model: t('activerecord.models.book')), edit_book_path(@book) %> |
  <%= link_to t('button.back', model: t('activerecord.models.book')), books_path %>
  <%= button_to t('button.destroy', model: t('activerecord.models.book')), @book, method: :delete %>
</nav>
